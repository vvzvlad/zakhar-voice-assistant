# Apply to micro-wake-word/microwakeword/mixednet.py (flag-guarded; default off => v8 unaffected)
# 1) In model_parameters(), add:
#   parser_nn.add_argument("--angular_head", type=int, default=0,
#       help="AM-Softmax/CosFace angular head (L2-norm embedding + cosine classifier)")
# 2) Replace the final head:
#     net = tf.keras.layers.Flatten()(net)
#     if getattr(flags, "angular_head", 0):
#         net = tf.keras.layers.UnitNormalization(axis=-1, name="l2norm")(net)
#         net = tf.keras.layers.Dense(1, use_bias=False, name="cos_logit")(net)
#         net = tf.keras.layers.Activation("sigmoid", name="prob")(net)
#     else:
#         net = tf.keras.layers.Dense(1, activation="sigmoid")(net)
# Trainer (am_train.py) projects the cos_logit kernel to norm s each step => raw logit = s*cos(theta);
# additive cosine margin applied in the LOSS only (z = raw - s*m*y) so inference stays standard.
